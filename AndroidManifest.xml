<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android">

    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />

    <application
        android:allowBackup="true"
        android:icon="@drawable/ic_launcher"
        android:label="@string/app_name"
        android:supportsRtl="true"
        android:usesCleartextTraffic="false"
        android:theme="@style/Theme.SauravDictionary">

        <!-- Home / how-to screen. Shows your HTML page in a WebView. -->
        <activity
            android:name=".MainActivity"
            android:exported="true"
            android:label="@string/app_name">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>

        <!--
            THIS is the part that makes "Saurav Popup Dictionary" appear in the
            text-selection menu next to Copy / Select all / Web search.

            android:label = the text shown in that menu. Android truncates long
            labels in the floating toolbar, so a short label such as
            "Saurav Dict" shows up better. Change @string/process_text_label.
        -->
        <activity
            android:name=".PopupDictionaryActivity"
            android:exported="true"
            android:label="@string/process_text_label"
            android:icon="@drawable/ic_launcher"
            android:theme="@style/Theme.SauravDictionary.Popup"
            android:excludeFromRecents="true"
            android:launchMode="singleTask"
            android:taskAffinity=""
            android:configChanges="orientation|screenSize|keyboardHidden">
            <intent-filter>
                <action android:name="android.intent.action.PROCESS_TEXT" />
                <category android:name="android.intent.category.DEFAULT" />
                <data android:mimeType="text/plain" />
            </intent-filter>
        </activity>

    </application>
</manifest>
